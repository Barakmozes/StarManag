overwrite: true
require:
  - dotenv/config
schema:
  - http://localhost:3000/api/graphql:
      headers:
        Authorization: "Bearer ${GRAPHQL_API_KEY}"
documents: "./graphql/files/**/*.graphql"
generates: 
  graphql/generated.ts:
    plugins:
      - typescript
      - typescript-operations
      - typescript-urql